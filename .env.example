PORT=5000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# JWT (obligatorio en producción)
JWT_SECRET=
JWT_EXPIRES_IN=7d

# CORS: orígenes permitidos separados por coma (vacío = permisivo en desarrollo)
# CORS_ORIGIN=http://localhost:8081,http://127.0.0.1:8081

# IA / RAG
USE_VECTOR_SEARCH=false
LLM_PROVIDER=openai
OPENAI_API_KEY=
GEMINI_API_KEY=
